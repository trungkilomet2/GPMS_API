version: "3.9"

services:
  gpms-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gpms-api
    ports:
      - "5229:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development